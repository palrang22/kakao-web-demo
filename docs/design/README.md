# 디자인 시안

Claude web 등에서 받아온 **원본 HTML 시안**을 여기에 둔다.
빌드에 포함되지 않는 참고용 자료다.

```
docs/design/
  hub.html          예: 허브 페이지 시안
  omni-flash.html   예: 체험 1번 시안
```

작업 흐름: 시안을 여기 저장 → React 컴포넌트(`src/`)로 옮김 → 시안은 그대로 남겨둔다.
나중에 "원래 디자인이 어땠더라" 확인할 때 쓴다.
